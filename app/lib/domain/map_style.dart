/// How the map draws its ground: the reduced vector style of the design board
/// or plain OpenStreetMap raster tiles.
enum MapStyle { reduced, rasterOsm }

const defaultMapStyle = MapStyle.reduced;
