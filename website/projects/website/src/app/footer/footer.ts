import { Component } from '@angular/core';
import { Logo } from '../logo/logo';

@Component({
  selector: 'bfs-fw-footer',
  imports: [Logo],
  templateUrl: './footer.html',
})
export class Footer {}
