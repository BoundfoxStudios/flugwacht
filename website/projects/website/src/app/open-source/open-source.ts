import { Component } from '@angular/core';
import { FaIconComponent } from '@fortawesome/angular-fontawesome';

@Component({
  selector: 'bfs-fw-open-source',
  imports: [FaIconComponent],
  templateUrl: './open-source.html',
})
export class OpenSource {}
